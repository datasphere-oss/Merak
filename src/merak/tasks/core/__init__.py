"""
These Task classes are "core" in the sense that they are used elsewhere in merak.
"""

import merak.tasks.core.collections
import merak.tasks.core.constants
import merak.tasks.core.function
import merak.tasks.core.operators
import merak.tasks.core.resource_manager
